server:
  port: 8087

spring:
  kafka:
    bootstrap-servers: localhost:9092

  boot:
    admin:
      client:
        url: http://localhost:1111

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

  endpoint:
    health:
      show-details: always
